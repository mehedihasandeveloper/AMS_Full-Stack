import { BanglaNumberPipe } from './bangla-number.pipe';

describe('BanglaNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new BanglaNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
